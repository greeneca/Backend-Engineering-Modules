name: Run Tests

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container: golang:1.25.1

    services:
      database:
        image: cassandra:latest
        ports:
          - 9042:9042
        options: >-
          --health-cmd="nodetool status"
          --health-interval=30s
          --health-timeout=10s
          --health-retries=5
        env:
          CASSANDRA_BROADCAST_ADDRESS: database

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install dependencies
        run: go mod download

      - name: Init Database
        run: |
          sudo snap install cqlsh
          ./data/scripts/init.sh database

      - name: Run tests
        run: go test -v ./...

